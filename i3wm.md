```
mode + enter     打开一个新窗口(默认横向展开，通过提前按 mode + v/h 可以设置展开方向)
mode + jki;      navigate between windows
mode + w/e       全屏/退出全屏
mode + s         stack windows
mode + shift +q  关闭窗口

mode + 3         跳到workspace 3
mode + shift +3  将当前 window 移动到 workspace 3

mode + d         launcher
```
